# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Scraper output: datasets are reproducible, assets are large binaries.
/data/**/assets/
/.cache/

# Client extraction: the JSON datasets are versioned, the binaries they name are not.
# Models, textures and sounds come out of the user's own install and are large.
/data/client/assets/

# The builder's icon sprite sheet: 5.9 MB of binary, and a montage of client icons
# we already extract individually. Archived outside the repo.
/data/_assets/

# OS / editor
.DS_Store
.idea/
.vscode/
