"""Extract and normalize Conquest of Azeroth talent data from the Ascension builder."""

__version__ = "0.1.0"

# Version of *our* normalized schema, not the upstream one. Bump on breaking
# changes to the exported JSON so old datasets stay identifiable.
SCHEMA_VERSION = 1
